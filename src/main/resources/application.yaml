server.servlet.context-path: /editor-emendas

lexml-jsonix.usar-entrada-saida-padrao-durante-conversao: false

spring.mail.host: smtp-appl.senado.gov.br

spring.servlet.multipart:
  max-file-size :  5MB
  max-request-size :  5MB

management.endpoint.health.probes.enabled :  true
management.health:
  livenessState.enabled: true
  readinessState.enabled: true
management.endpoints.web.base-path: /sfstatus

url:
  lexeditweb: ${URL_LEXEDITWEB:https://legis.senado.leg.br/lexeditweb}
  servico.permite_texto_livre: /resources/proposicao/permite-texto-livre
